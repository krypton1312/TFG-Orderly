---
phase: 12-app-configuration
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - frontend/orderly/src/main/resources/com/yebur/portal/portal-dark.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "When portal-dark.css is added to scene stylesheets, sidebar background turns #111315"
    - "When portal-dark.css is active, main content area background turns #1a1c1e"
    - "When portal-dark.css is active, card components show #1f2124 background and #2a2d31 border"
    - "When portal-dark.css is active, primary text turns #e2e8f0; muted text turns #64748b"
    - "When portal-dark.css is active, SMTP and config form fields show #272b2f background"
    - "portal-dark.css contains zero CSS var() calls — JavaFX 2.1 subset does not support them"
  artifacts:
    - path: "frontend/orderly/src/main/resources/com/yebur/portal/portal-dark.css"
      provides: "All 30+ dark selectors covering sidebar, header, cards, forms, modals, config section overrides"
  key_links:
    - from: "ConfiguracionController.applyTheme(true)"
      to: "portal-dark.css"
      via: "scene.getStylesheets().add(getClass().getResource('/com/yebur/portal/portal-dark.css').toExternalForm())"
      pattern: "portal-dark.css"
---

<objective>
Dark theme stylesheet for the PC portal: create portal-dark.css with all 30+ dark-mode
selectors from UI-SPEC. File is loaded at runtime by PortalController and
ConfiguracionController when theme="dark" — it overrides the existing light theme
selectors by specificity via the cascade.

Purpose: Implements D-02 (dark theme toggle) — actual visual feedback for the
RadioButton dark/light switch implemented in Plan 04.
Output: Single CSS file; no Java changes required.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-app-configuration/12-CONTEXT.md
@.planning/phases/12-app-configuration/12-UI-SPEC.md

<interfaces>
<!-- Read these files BEFORE creating portal-dark.css -->

frontend/orderly/src/main/resources/com/yebur/portal/portal.css — read FULL FILE
  Reason: identify EVERY existing CSS class/selector that needs a dark override.
  Look for: .sidebar, .dashboard-header, .main-title, .subtitle, .info-card, .report-ops-card,
  .stat-card, .section-title, .info-label, .info-value, .mini-value, .month-label,
  .stat-card-label, .stat-card-value, .form-label, .form-textfield, .form-combobox,
  .prev-month-label, .modal-title, .modal-card, .modal-header, .list-card, .list-card-text,
  .bottom-section, .nav-item, .nav-item-selected, .content-area, .report-scroll

JavaFX CSS constraint (CRITICAL):
  JavaFX uses CSS 2.1 subset — NO CSS custom properties (var()), NO calc(), NO :is(), NO :where()
  All values MUST be hardcoded hex or rgba() literals.
  Use -fx-background-color, -fx-text-fill, -fx-border-color etc. (JavaFX properties).
  Do NOT use: background-color, color, border-color (CSS web properties — ignored by JavaFX).

Dark palette (authoritative — copy exactly):
  Root bg:         #1a1c1e
  Sidebar/header:  #111315
  Sidebar border:  #2a2d31
  Card bg:         #1f2124
  Card border:     #2a2d31
  Form field bg:   #272b2f
  Primary text:    #e2e8f0
  Secondary text:  #94a3b8
  Muted text:      #64748b
  Form text:       #e2e8f0
  Accent:          #ea580c  (unchanged from light)
  Accent hover:    #fb923c  (lighter on dark)
  Sidebar hover:   rgba(234,88,12,0.12)
  Sidebar selected: rgba(234,88,12,0.22)
  Status success:  #22c55e
  Status error:    #f87171
  ScrollBar track: #272b2f
  ScrollBar thumb: #3a3d42
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portal-dark.css with all dark selectors</name>
  <read_first>
    - frontend/orderly/src/main/resources/com/yebur/portal/portal.css — FULL FILE (enumerate all existing selectors that need dark overrides)
    - frontend/orderly/src/main/resources/com/yebur/portal/views/configuracion.css — light theme classes (need dark counterparts here too)
    - .planning/phases/12-app-configuration/12-UI-SPEC.md — dark selector list (authoritative)
  </read_first>
  <files>
    frontend/orderly/src/main/resources/com/yebur/portal/portal-dark.css
  </files>
  <action>
Create `portal-dark.css`. Every selector must:
1. Use JavaFX CSS properties (-fx-* prefix), NOT web CSS properties
2. Use hardcoded hex/rgba values — ZERO var() calls
3. Override existing portal.css selectors via the scene stylesheet cascade

```css
/* ================================================================
   portal-dark.css — Dark Theme Override
   Phase 12: App Configuration (D-02)
   
   LOADED AT RUNTIME via scene.getStylesheets().add(...)
   Overrides portal.css light theme via CSS cascade order.
   
   CONSTRAINT: JavaFX CSS 2.1 subset — NO var(), NO calc(), NO :is()
   ALL values are hardcoded literals.
   ================================================================ */

/* ---- ROOT / SCENE BACKGROUND ---- */
.root {
    -fx-background-color: #1a1c1e;
}

/* ---- SIDEBAR ---- */
.sidebar {
    -fx-background-color: #111315;
    -fx-border-color: #2a2d31;
}

/* Nav items in sidebar */
.sidebar .button,
.sidebar .nav-item {
    -fx-text-fill: #cbd5e1;
    -fx-background-color: transparent;
}

.sidebar .button:hover,
.sidebar .nav-item:hover {
    -fx-background-color: rgba(234,88,12,0.12);
    -fx-text-fill: #fb923c;
}

.sidebar .button.nav-item-selected,
.sidebar .nav-item.nav-item-selected,
.sidebar .button:selected,
.nav-item-selected {
    -fx-background-color: rgba(234,88,12,0.22);
    -fx-text-fill: #fb923c;
}

/* ---- DASHBOARD HEADER ---- */
.dashboard-header {
    -fx-background-color: #111315;
    -fx-border-color: #2a2d31;
}

.dashboard-header .main-title {
    -fx-text-fill: #e2e8f0;
}

/* ---- TYPOGRAPHY ---- */
.main-title {
    -fx-text-fill: #e2e8f0;
}

.subtitle {
    -fx-text-fill: #94a3b8;
}

.description {
    -fx-text-fill: #64748b;
}

.section-title {
    -fx-text-fill: #64748b;
}

/* ---- CARDS ---- */
.info-card,
.report-ops-card,
.stat-card {
    -fx-background-color: #1f2124;
    -fx-border-color: #2a2d31;
}

.list-card {
    -fx-background-color: #1f2124;
    -fx-border-color: #2a2d31;
}

.list-card-text {
    -fx-text-fill: #e2e8f0;
}

/* ---- CARD LABELS / VALUES ---- */
.info-label {
    -fx-text-fill: #64748b;
}

.info-value {
    -fx-text-fill: #e2e8f0;
}

.mini-value {
    -fx-text-fill: #e2e8f0;
}

.month-label {
    -fx-text-fill: #e2e8f0;
}

.prev-month-label {
    -fx-text-fill: #64748b;
}

.stat-card-label {
    -fx-text-fill: #64748b;
}

.stat-card-value {
    -fx-text-fill: #e2e8f0;
}

/* ---- CONTENT AREA ---- */
.content-area {
    -fx-background-color: #1a1c1e;
}

/* ---- FORM ELEMENTS (general — dataOperation, listados, etc.) ---- */
.form-label {
    -fx-text-fill: #64748b;
}

.form-textfield {
    -fx-background-color: #272b2f;
    -fx-text-fill: #e2e8f0;
    -fx-border-color: #2a2d31;
}

.form-textfield:focused {
    -fx-border-color: #ea580c;
    -fx-background-color: #1f2124;
}

.form-combobox {
    -fx-background-color: #272b2f;
    -fx-border-color: #2a2d31;
}

.form-combobox .list-view {
    -fx-background-color: #1f2124;
}

.form-combobox .list-cell {
    -fx-text-fill: #e2e8f0;
    -fx-background-color: #1f2124;
}

.form-combobox .list-cell:hover {
    -fx-background-color: rgba(234,88,12,0.12);
    -fx-text-fill: #fb923c;
}

/* ---- MODAL ---- */
.modal-card {
    -fx-background-color: #1f2124;
    -fx-border-color: #2a2d31;
}

.modal-header {
    -fx-background-color: #111315;
    -fx-border-color: #2a2d31;
}

.modal-title {
    -fx-text-fill: #e2e8f0;
}

.modal-body {
    -fx-background-color: #1f2124;
}

/* ---- BOTTOM SECTION (sidebar footer) ---- */
.bottom-section {
    -fx-border-color: #2a2d31;
}

/* ---- SCROLL PANE ---- */
.report-scroll {
    -fx-background-color: transparent;
    -fx-background: #1a1c1e;
}

.report-scroll > .viewport {
    -fx-background-color: transparent;
}

.report-scroll .scroll-bar {
    -fx-background-color: #272b2f;
}

.report-scroll .scroll-bar .thumb {
    -fx-background-color: #3a3d42;
    -fx-background-radius: 4px;
}

/* ---- CONFIGURACION SECTION DARK OVERRIDES ---- */

.config-separator {
    -fx-background-color: #2a2d31;
}

.config-form-label {
    -fx-text-fill: #64748b;
}

.config-textfield {
    -fx-background-color: #272b2f;
    -fx-border-color: #2a2d31;
    -fx-text-fill: #e2e8f0;
}

.config-textfield:focused {
    -fx-border-color: #ea580c;
    -fx-background-color: #1f2124;
}

.config-combobox {
    -fx-background-color: #272b2f;
    -fx-border-color: #2a2d31;
}

.config-combobox .list-view {
    -fx-background-color: #1f2124;
}

.config-combobox .list-cell {
    -fx-text-fill: #e2e8f0;
    -fx-background-color: #1f2124;
}

.config-combobox .list-cell:hover {
    -fx-background-color: rgba(234,88,12,0.12);
    -fx-text-fill: #fb923c;
}

.config-secondary-btn {
    -fx-background-color: #1f2124;
    -fx-border-color: #2a2d31;
    -fx-text-fill: #cbd5e1;
}

.config-secondary-btn:hover {
    -fx-background-color: rgba(234,88,12,0.12);
    -fx-text-fill: #fb923c;
    -fx-border-color: rgba(234,88,12,0.30);
}

.smtp-status-testing {
    -fx-text-fill: #64748b;
}

.smtp-status-success {
    -fx-text-fill: #22c55e;
}

.smtp-status-error {
    -fx-text-fill: #f87171;
}
```

**After creating the file:**
1. Run `grep -c "var(" frontend/orderly/src/main/resources/com/yebur/portal/portal-dark.css`
   → MUST output 0. If any var() found, replace with hardcoded equivalents.
2. Run `grep -n "background-color:" frontend/orderly/src/main/resources/com/yebur/portal/portal-dark.css`
   → MUST output 0. Only `-fx-background-color` is valid. Fix any plain `background-color` lines.
3. Read the actual `portal.css` and add dark overrides for ANY classes present there that are
   NOT already covered in this file. Better to over-include than miss a selector.
  </action>
  <verify>
    <automated>powershell -Command "if ((Select-String -Pattern 'var\(' -Path 'frontend/orderly/src/main/resources/com/yebur/portal/portal-dark.css').Count -eq 0) { Write-Output 'PASS: no var()' } else { Write-Output 'FAIL: var() found'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `frontend/orderly/src/main/resources/com/yebur/portal/portal-dark.css`
    - `grep -c "var(" portal-dark.css` returns 0
    - `grep -c "background-color:" portal-dark.css` (without -fx- prefix) returns 0
    - File contains at minimum: `.root`, `.sidebar`, `.dashboard-header`, `.info-card`, `.modal-card`, `.form-textfield`, `.config-textfield`, `.config-separator`, `.smtp-status-success`, `.smtp-status-error`, `.nav-item-selected`
    - PC module compiles with exit 0 (`mvn clean compile -DskipTests -f frontend/orderly/pom.xml`)
  </acceptance_criteria>
  <done>portal-dark.css created with 30+ dark selectors, zero CSS var() calls, all JavaFX -fx- properties.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS file → Scene stylesheets | CSS loaded from classpath (compile-time constant path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Tampering | portal-dark.css classpath resource | accept | Resource loaded by path literal `/com/yebur/portal/portal-dark.css`; path is a compile-time constant from packaged JAR; no user-controlled CSS injection possible |
| T-12-17 | Information Disclosure | CSS file exposes component class names | accept | CSS class names are reverse-engineerable from the JAR in any case; no sensitive data in class selectors |
</threat_model>

<verification>
Dark theme CSS complete when:
- File exists and compiles into JAR (mvn compile exits 0)
- Zero var() occurrences (grep count = 0)
- Zero non-prefixed background-color / color / border-color (grep count = 0 for each)
- Visual: toggling "Oscuro" radio in the running app noticeably darkens the portal
</verification>

<success_criteria>
- portal-dark.css contains all required dark selectors from UI-SPEC
- No CSS var() or non-JavaFX property names
- PC module compiles with exit 0
- Dark theme visually applies when portal-dark.css is active on the scene
</success_criteria>

<output>
After completion, create `.planning/phases/12-app-configuration/12-05-SUMMARY.md`
</output>
