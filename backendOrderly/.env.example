# Copy these variable names and set real values in your IDE run configuration or OS environment.
# Generate JWT_SECRET with: openssl rand -base64 32  (minimum 32 characters required)

JWT_SECRET=replace-with-at-least-32-random-characters-here
DB_PASSWORD=your_postgres_password_here
